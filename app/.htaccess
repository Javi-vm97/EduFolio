# EduFolio - Bloquea el acceso web directo a la logica de la aplicacion
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
